[project]
name = "medical-electronic-system"
version = "0.1.0"
description = "UK GP Medical Records System"
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "duckdb",
    "python-multipart",
    "jinja2",
]